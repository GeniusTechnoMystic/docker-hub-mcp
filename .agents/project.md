# docker-hub-mcp

## Description
MCP server for Docker Hub Image Stats — 4 tools wrapping the public Docker Hub API.

## Tasks
- Add new API endpoints as new tools in `src/docker_hub_mcp/server.py`
- Keep test coverage for each tool
- Bump version before tagging for release
